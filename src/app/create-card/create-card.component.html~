<h3>card creation page</h3>

<div>

    <div class="form-group col-md-8">
        <label >Card's name</label>
        <input [(ngModel)]="card.name" placeholder="type">
        <label >Type</label>
        <font size="2"> 
            (<input [(ngModel)]="card.type" placeholder="type"> 
            ):
        </font>
        <label >Word: </label>
        <input [(ngModel)]="card.word" placeholder="word">
        <br/>
    </div>
 
        
    
    <div class="form-group col-md-4">
        <label for="exampleInputFile">import image</label>
        <input (change)="imageChange($event)"  type="file" id="exampleInputFile" accept=".png,.jpg">
        <p class="help-block">add an image maybe?</p>
        <div *ngIf="image">
            <img [src]="image"/>
        </div>
    </div>
    
    <button (click)="addTranslation()" type="button" class="btn btn-default">Add a translation</button>
    <div class="translationdiv" *ngFor="let t of card.translation; let i = index">
        <button (click)="removeTranslation(i)" type="button" class="close" aria-label="Close"><span aria-hidden="false">&times;</span></button>
        <font size="2" >
            (<input [(ngModel)]="t.info" placeholder="some more info"> ): 
        </font>
        <input [(ngModel)]="t.translation" placeholder="translation">
        <br/><div>
            <div  class="col-md-6">
                <label>en: </label>{{t.example.en}}
                <input [(ngModel)]="t.example.en" placeholder="english sentence">
            </div>
            <div class="col-md-6">
                <label>ru: </label> 
                <input [(ngModel)]="t.example.ru" placeholder="фраза на русском">
            </div> 
        </div>
        
    </div>

</div>
<button (click)="submit()" type="submit" class="btn btn-default">Finish</button>
