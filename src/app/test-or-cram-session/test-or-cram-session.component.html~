
<button *ngFor="let card of cards; let i = index" (click)="openCard(card, i)" class="btn btn-default col-md-3"
    >
    <span class="label label-default" *ngIf="answered[i]">answered</span>
    {{card.word}}
</button>


<div id="cardView" *ngIf="state != 0" class=" jumbotron">
    <h3>{{selectedCard.word}}</h3>

    <button *ngIf="state == 1" class="btn btn-default" (click)="seeCard()">see card</button>
    
    <div *ngIf="state == 2" >
        <div *ngFor="let t of selectedCard.translation">
            <label><span class="label label-info">Translation:</span> {{t.translation}}</label>
            <div>
                <label><span class="label label-info">en:</span>{{t.example.en}}</label>
                <label><span class="label label-info">ru:</span>{{t.example.ru}}</label>
            </div>
        </div>
    </div>
    <div class="btn-group" role="group" *ngIf="state == 2">
        <button class="btn " (click)="answerCard(selectedCard,'easy')">easy</button>
        <button class="btn " (click)="answerCard(selectedCard,'ok')">ok</button>
        <button class="btn " (click)="answerCard(selectedCard,'hard')">hard</button>
    </div>    
</div>

<div class="col-md-12">
    <bs-progress>
        <bar [value]="hard" type="danger">
            <span *ngIf="hard > (max/20)">{{hard}}</span>
        </bar>
        <bar [value]="ok" type="info">
            <span *ngIf="ok > (max/20)">{{ok}}</span>
        </bar>
        <bar [value]="easy" type="success">
            <span *ngIf="easy > (max/20)">{{easy}}</span>
        </bar>
    </bs-progress>
</div>
